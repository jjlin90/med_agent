# 文档核查范围与源码依据

核对日期：2026-09-15。对象为桌面主文档、逐点精讲、面试全解、评估说明、两张流程图和阅读入口。正文以当前 Deep Agents 项目叙述，保留章节、工具讲解、原理与面试问答。

| 核对主题 | 主要源码 | 已按源码明确的行为 |
|---|---|---|
| 编排与模式 | medical/deep_agent.py、medical/graph.py | 工厂创建；五类钩子；2/8 工具；条件 tool_choice |
| 安全与抽取 | medical/compliance.py、medical/state.py、medical/graph.py | 23 条急症、6 条诊断、9 条用药规则；SafetyIntent 全量语义兜底；抽取与路由条件 |
| 工具参数 | medical/tools.py | 注入字段与模型参数不同；计划枚举不验证业务完成 |
| 工具批次 | before_model、record_tool_round_node、agent_limit_node | 新结果一批记一次；第六批后硬停止；审核可覆盖收束文案 |
| 输出证据 | output_check_node、audit_output | 本轮 RAG 来源；按消息名称分支；无成功/语义核验 |
| 上传和隐私 | medical/uploads.py、medical/privacy.py、medical/secure_store.py | 10 MiB 解码量；thread 路径；同名覆盖；有限脱敏与审计加密 |
| 检索与建库 | medical/rag/retriever.py、html_parser.py、build_index.py | 顺序双召回；RRF；条件重排；切片例外与双索引非原子 |
| 入口与恢复 | main.py、app.py、langgraph.json、medical/api.py、前端 Stream.tsx | SQLite 显式注入；服务端管理；刷新恢复条件；浅层 health |
| 评估 | evaluate.py、data/eval_dataset.json、data/eval_report.md | 代理计分、样本数、报告存档范围与上传协议缺口 |
| 重试 | 已安装 langchain/agents/middleware/model_retry.py、_retry.py | 默认退避与抖动、可重试异常；底层 SDK 调用次数另计 |

本次在当前项目 .venv 中运行 unittest discover：98 项通过，其中包含高精度安全规则、全量结构化领域与安全意图分类、低置信/异常降级、非医疗固定回复、寒暄固定回复和危急优先级覆盖，工具输出为 Ran 98 tests / OK。核心 44、能力 26、指标与脚本轨迹 15、日志 2、SDK runtime 10、Streamlit 契约 1。真实 SDK 集成测试使用受控模型，没有重新执行付费真实模型 full 评估，也没有本次浏览器端 HTTP 部署验收结论。

已保存评估数字来自 data/eval_report.md；报告没有完整工具消息、回答原文以及模型/代码版本绑定。本次核对不能反向补出这些缺失证据。未来方案以待实现或待实测标明。

验证另包括 Markdown 代码围栏/链接锚点、JSON 示例、SVG XML、文件一致性和备份完整性检查。文本扫描只用于发现遗漏，不作为语义正确的唯一证据；本次更改以源码分支核对为依据。有限测试也不证明所有模型输入、运行异常和临床场景均正确。

框架概念参考 [Deep Agents 官方说明](https://docs.langchain.com/oss/python/deepagents/overview)：create_deep_agent 来自 deepagents 库，使用 LangGraph 运行时；具体项目行为以本地安装源码为准。BGE-M3 的多语言、多功能、多粒度定义参考 [原论文](https://arxiv.org/abs/2402.03216)，本项目只使用其 dense 接口，BM25 独立实现。
